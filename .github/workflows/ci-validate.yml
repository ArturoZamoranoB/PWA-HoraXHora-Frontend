name: Validate & Test Before Merge

on:
  pull_request:
    branches: ["main"]

jobs:
  validate:
    name: Validate & Test Before Merge 
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Run unit tests
        run: npm test --if-present

      - name: Build project
        run: npm run build
